legacy-peer-deps=true
save-exact=true

#Suppresses strict peer dependency resolution (like npm 6/7) — avoids errors like you're seeing locally
#When you install new packages, locks exact version ("1.2.3" instead of "^1.2.3") — useful for tight control
